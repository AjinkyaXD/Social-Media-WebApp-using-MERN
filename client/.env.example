# Set this when the frontend is deployed to a different origin than the backend.
# Leave empty to use same-origin relative URLs (recommended for single-host deploys).
REACT_APP_API_URL=https://social-media-webapp-using-mern.onrender.com
